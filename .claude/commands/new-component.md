Create a new React component called: $ARGUMENTS

Rules from CLAUDE.md:
- Mobile-first, max-w-md mx-auto
- Tailwind only, no custom CSS
- All API calls through src/api/api.js (add the needed functions there too)
- Add the component to the correct page in src/pages/
- Use existing components as style reference (look at FoodLogger.jsx first)
- Include loading state, error state, and empty state
- Export as default

Create the component file, update api.js, and update the relevant page.
